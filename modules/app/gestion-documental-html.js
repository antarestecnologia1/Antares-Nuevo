/**
 * OBSOLETO — el expediente por colaborador fue reemplazado por el DMS corporativo.
 * La UI viva está en `gestion-documental.js` (company documents).
 * Este archivo se conserva vacío a propósito para no romper imports residuales.
 */
export function renderDocumentsShell() {
  return "";
}
