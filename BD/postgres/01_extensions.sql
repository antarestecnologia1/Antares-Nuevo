-- Antares BD — Extensiones (PostgreSQL 15+)
-- gen_random_uuid(): en PG 13+ suele estar disponible sin extensión.
-- Si su instalación lo requiere, descomente:
-- CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- Opcional: emails case-insensitive
-- CREATE EXTENSION IF NOT EXISTS citext;
