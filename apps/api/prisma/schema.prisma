// Esquema DEMO para Nest (User, TripRequest en inglés).
// Si la base ya tiene el esquema español de BD/postgres (usuarios, solicitudes_transporte, …),
// no ejecute prisma migrate contra esa misma base: use solo los scripts SQL del repo o alinee modelos aparte.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


enum Role {
  ADMIN
  CLIENT
  RRHH
}

enum RequestStatus {
  PENDIENTE
  APROBADA
  EN_TRANSITO
  COMPLETADA
  CANCELADA
  RECHAZADA
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         Role     @default(CLIENT)
  refreshToken String?
  requests     TripRequest[]
  createdAt    DateTime @default(now())
}

model TripRequest {
  id               String        @id @default(cuid())
  userId           String
  user             User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  origin           String
  destination      String
  vehicleType      String
  weightKg         Int
  pickupAt         DateTime
  status           RequestStatus @default(PENDIENTE)
  approvalDeadline DateTime
  createdAt        DateTime      @default(now())
}

model AuditLog {
  id        String   @id @default(cuid())
  actorId   String
  action    String
  entity    String
  payload   String
  createdAt DateTime @default(now())
}
