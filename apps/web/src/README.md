# `apps/web/src`

Las vistas del portal estático **no** están aquí: se cargan desde la raíz del monorepo en `modules/portal/views/` (ver `index.html` en la raíz).

La app Next.js usa principalmente `apps/web/app/` y `apps/web/components/`.
