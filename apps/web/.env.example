# Next.js (apps/web) — copiar a .env.local
#
# URL del backend Nest (prefijo /api lo añade auth-provider)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Supabase (anon + RLS desde el cliente Next). Project URL sin /rest/v1.
# NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
